# Version 1 archive

This archive preserves the earlier loose visual attempt for comparison only.

It was replaced because it did not follow the full repurpose package contract:

- images were stored in `visuals/` instead of `drafts/<Platform>/images/`
- the package did not include 20 PNGs
- the package did not include the LinkedIn GIF
- the package did not include `preview/image-qa.md`
- the manifest did not include generated media counts

The active v2 package is the source of truth.
