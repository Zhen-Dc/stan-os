# How To Get These Images Into Penpot

The files are already created on disk, but Penpot will not show them in the
dashboard or Drafts automatically.

You must import them into a Penpot file.

## Fastest Method

1. Open Penpot:

```text
http://localhost:9002
```

2. Log in if needed:

```text
Email: stan@local.penpot
Password: StanLocal2026!
```

3. In Penpot, create a new design file named:

```text
Claude AI Marketer Social Graphics
```

4. Open this folder in Windows:

```text
C:\Users\DELL\Master Project\Stan OS\AIS-OS\asset-folder\penpot-exports\turn-claude-into-your-ai-marketer-in-15-minutes\_flat-import
```

5. Select all SVG files in that folder.

6. Drag them onto the open Penpot canvas.

7. Penpot should import them as editable designs. Text, cards, badges, and
shapes should be editable. Stanley's avatar is embedded as an image layer.

## Import Pack

If you prefer one file, use this zip:

```text
C:\Users\DELL\Master Project\Stan OS\AIS-OS\asset-folder\penpot-exports\turn-claude-into-your-ai-marketer-in-15-minutes\penpot-editable-svg-import-pack.zip
```

If Penpot does not accept the zip directly, unzip it and drag the SVG files from
`_flat-import` onto the canvas.

## Important

Do not look for these in Penpot Drafts before importing. They are local files
until you drag or import them into a Penpot file.

The source platform folders are also available here:

```text
C:\Users\DELL\Master Project\Stan OS\AIS-OS\asset-folder\penpot-exports\turn-claude-into-your-ai-marketer-in-15-minutes
```

## After Editing

When you finish manual edits in Penpot, export final PNGs back into:

```text
C:\Users\DELL\Master Project\Stan OS\AIS-OS\asset-folder\content\turn-claude-into-your-ai-marketer-in-15-minutes\drafts\<Platform>\images
```

Use the existing PNG filenames if you want to replace the current draft images.
